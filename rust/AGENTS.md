# Agent Guidelines

See `/AGENTS.md` for shared rules.

Local tests use `TestRelay`/`TestServer` and no network deps; reserve `#[ignore]` for external infra.

Keep it simple: no over-engineering, minimal changes that solve the problem.
