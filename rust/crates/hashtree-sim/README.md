# hashtree-sim

This private test-only crate contains a small loopback Nostr relay used by
resolver and updater integration tests. The retired Hashtree mesh transport,
blob framing, peer selector, and pubsub simulation are intentionally absent.
