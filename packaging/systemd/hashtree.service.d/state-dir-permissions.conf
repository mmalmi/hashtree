[Service]
# The state directory may be an external filesystem mounted before the daemon
# starts. Recreate/chown the mount root as root so the unprivileged service user
# can open the store after reboots and remounts.
ExecStartPre=+/usr/bin/install -d -o hashtree -g hashtree -m 0755 /srv/hashtree/state
